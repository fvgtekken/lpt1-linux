// Creá una función llamada getColor que retorne el string "blue".

//Después, invocá esa función y guardá lo que retorna en una variable llamada result.

//Finalmente, mostrá result en la consola.

function getColor() {
    return 'blue'
}
let result = getColor();
console.log(result);
