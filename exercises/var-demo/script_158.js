let nombre = 'Natalia'
const saludos = ['hello', nombrar, nombre, 'hi'];

for (let i = 0; i < 3; i++) {
    if (i !== 1) {
        console.log(saludos[i]);
    } else {
      console.log(nombrar(nombre));
    }
}

function nombrar(nombre) {
    return nombre

}