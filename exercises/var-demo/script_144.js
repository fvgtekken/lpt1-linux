const colours = ['red', 'blue', 'green', 'pink']

const colourFirst = colours[0];
const colourSecond = colours[3];

if (colourFirst === 'red' && colourSecond !== 'green') {
    console.log('combinación válida');
}