const minimumSpeed = function keep(value) {
    return value
}
let velocity = minimumSpeed(50);
console.log(velocity);

const averageSpeed = function keep(value) {
    return value
}
velocity = averageSpeed(90);
console.log(velocity);
