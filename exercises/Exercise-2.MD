1 - Explicar la Diferencia entre git merge <branch> y git cherry-pick <commit>
2 - Hacer un ejercicio donde tengas que aplicar cherry-pick :

3 - Crear un branch llamado add-cherry-pick aqui vas a crear un texto que explique que es cherry-pick y para que es su uso, una vez que termines creas un commit
creas un archivo de texto que contiene la info del comando cherry-pick y para que sirve

4 - Crear un branch que se llama add-branch-concept creas un commit con el texto que explica para que es un merge
creas un archivo que explica para que es el merge y los comandos mas basicos

5 Crear otro branch que se llame concept-cherry-pick-merge aqui usando EL COMANDO cherry-pick para obtener en esta rama la info de ambas ramas.
