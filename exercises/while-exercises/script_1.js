let i = 0; // inicio (antes de while)

while (i < 4) {  // condición (se revisa antes de cada vuelta)
    console.log(i); 
    i++;            // incremento (uno lo escribe a mano adentro)
}

//es lo mismo que hacer esto:

for (let i = 0; i < 4; i++) {
  console.log(i);
}

