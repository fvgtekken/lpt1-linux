Red

ip addr — Muestra direcciones IP.
ip route — Muestra la tabla de rutas.
ifconfig — Muestra o configura interfaces de red (legacy).
route — Muestra o configura la tabla de ruteo (legacy).
netstat — Muestra conexiones de red (legacy).
ss — Muestra sockets.
ping — Comprueba conectividad con otro host.
host — Resuelve nombres DNS.
